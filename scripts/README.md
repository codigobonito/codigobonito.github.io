## Configurar o ambiente para rodar localmente

Usando o [venv](https://docs.python.org/3/library/venv.html) do Python 3:

```sh
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```